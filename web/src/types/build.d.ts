/**
 * Build stamp injected by `vite.config.ts` at build time (see `buildStamp`).
 * Always defined — `define` substitutes it as a literal, so there is no
 * runtime fallback to guard against.
 */
declare const __BUILD_ID__: string;
